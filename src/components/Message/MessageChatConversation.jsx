import React from "react";

const MessageChatConversation = (props) => {
  return (
    <div>
      senderuuid={props.senderUUID}, receiverUUID={props.receiverUUID}
    </div>
  );
};

export default MessageChatConversation;
